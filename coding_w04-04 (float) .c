#include <stdio.h>

float main() {
    float score = 90.5;
    printf("Value = %f\n",score);
    return 0;
}
//ความแตกต่างระหว่างintกับfloat
//intจะแสดงได้แค่ตัวเลขที่เป็นจำนวนเต็มส่วนfloatจะแสดงค่าทศนิยมได้
//ถ้าใช้floatจะแสดงค่าได้จริงแต่ถ้าใช้intจะไม่สามารถแสดงผลลัพธ์ได้
